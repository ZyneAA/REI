expose class Comparable {

    // Get comparable from Obj that inherith this class
    get_comparable() {
        let is_exist = @exist("comparable");

        if (is_exist) {
            return this.comparable;
        }
        fatal "property 'comparable' not fonund";
    }

}

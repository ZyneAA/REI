expose class Drop {

    // Destroy an attribute from an obj
    // Unstable
    drop() {

        if (@exist("target")) {
            @destroy("target");
            return;
        } else {
            fatal "target not found";
        }

    }

}

expose class Fmt {

    // Experimental
    fmt() {

        if (@exist("fmttable")) {
            println this.fmttable;
        } else {
            fatal "Formattable not found";
        }

    }

}

expose class Fs {

    //Both functions are experimental

    static build_dir(path, recursive) {
        _Fs_build_dir(path, recursive);
    }

    static read_dir(path) {
        return _Fs_read_dir(path);
    }

}

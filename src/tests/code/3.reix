class Animal{
    make_sound() {
        println "brrrrr";
    }
}

let cat = Animal();
cat <- sound = "meow";
cat.sec_sound = "lol";
cat->make_sound();

println cat.sound;
println cat -> sec_sound;
println "cat: " + cat;

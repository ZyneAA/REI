fn fib(n) {

    if (n <= 1) {
        return n;
    }
    else {
        return fib(n-2) + fib(n-1);
    }

}

fn fac(n) {
  if (n <= 1) return 1;
  return n * fac(n - 1);
}

let a = fib(8);
println a;


expose class Aether {

    init() {
        this.app = _NET_router();
    }

    set_get(path, func) {
        _NET_router_get(this.app, path, func);
    }

    serve(host) {
        _NET_serve(this.app, host);
    }

}
